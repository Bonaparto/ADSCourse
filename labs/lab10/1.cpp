#include <iostream>
using namespace std;
// несложная задача на trie.
// вершиной будет город с минимальным кодом(вроде как первый в инпуте)
// для него есть 9 выходов(цифры 0-9)
// в зависимости от того, сколько у него есть продолжений,
// нужно будет отнимать от его изначального возможного количества
// разных номер по какой то степени 10 в зависимости от длины кода.


int main() {
    int n, m;
    cin >> n >> m;
    while(n--) {
        
    }
}