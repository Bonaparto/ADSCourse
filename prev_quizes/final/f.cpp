#include <fstream>
#include <cmath>
using namespace std;

ifstream fin("input.txt");
ofstream fout("output.txt");   

int main() {
    int n, l = 0, r = 0;
    fin >> n;
    int a[n];
    long long cur = 0;
    for(int i = 0; i < n; ++i) fin >> a[i];
    long long sum = 1e18;
    while(l != n) {
        
    }
}